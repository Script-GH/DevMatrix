{
  "name": "devpulse-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "vscode": "^1.85.0"
  },
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*",
    "shared"
  ],
  "scripts": {
    "scan": "npm --prefix packages/cli run dev -- scan",
    "dmx": "npm --prefix packages/cli run dev --"
  },
  "dependencies": {
    "ansi-escape": "^1.1.0",
    "ansi-escapes": "^7.3.0",
    "strip-final-newline": "^4.0.0"
  }
}
